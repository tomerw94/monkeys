import { Minion } from './minion';
import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class MinionService {

  constructor(
    // private name: string, private manaCost: number,
    // private text: string, private attack: number,
    // private health: number, private maxHealth: number,
    // private attacksLeftThisTurn: number, private attackTimesThisTurn: number
  ) {
    return {
      // name: name,
      // manaCost: manaCost,
      // text: text,
      // attack: attack,
      // health: health,
      // maxHealth: maxHealth,
      // attacksLeftThisTurn: attacksLeftThisTurn,
      // attackTimesThisTurn: attackTimesThisTurn
    }
  }

}
